https://www.youtube.com/watch?v=zk_zggxQsxQ&t=3697s


ACID
Atomicity -> rollback
Consistency -> constrains are always respected
Isolation -> transaction are independent
Durability ->

//---------

> what are the isolation problems?

46:47

> What is transaction
A set of sql operation executed as one unit

> When Indexes are not used
low cardinality (only few option), leading windcard, small table

> Indexing Strategy
which queries are frequent ? which columns appear in WHERE ? 
Which column appear in JOIN ? which column appear in ORDER BY

> First Normal Form (1NF)
Rule : atomic values, no repeating groups, each cell has a single value 

> 2NF
No partial dependency on composite key

what is partial dependency ?
-> When a column depends on part of a composite key

OrderItems
(order_id, product_id)  | product_name | quantity

here product_name depends on productId .. not full key

> 3NF
no transitive dependency

what is transitive dependency
-> non-key column depends on another non-key column

user_id | city | zipCode ...

here we can see zipCode depends on city .. which is a non key column .. 
so, we need to create separate table

> Self Join 2:31:31

Redis can handle millon QPS

Redis is much cheaper to scale
-> simple key value , no join, no SQL parsing, no locks

> Cache Eviction Policies
--> LRU ( Least recently used )
> Cache Write Strategies 
--> Cache-Aside (lazy loading)
    Write-Through Cache -> app write to cache, cache synchronously write to DB ** and for read, always read from cache .. 

write behind